{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the pet overlay window and per-peer overlays",
  "windows": [
    "pet",
    "peer-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
